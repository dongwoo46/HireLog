apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus # ← 추가
    access: proxy
    url: http://prometheus:9090
    isDefault: false
    editable: false

  - name: Loki
    type: loki
    uid: loki # ← 추가
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
