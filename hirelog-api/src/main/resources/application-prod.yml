spring:
  datasource:
    url: jdbc:postgresql://prod-db:5432/hirelog_prod
    username: hirelog
    password: ${DB_PASSWORD}

  flyway:
    enabled: true
    baseline-on-migrate: false   # ⭐ 중요
    validate-on-migrate: true

  jpa:
    hibernate:
      ddl-auto: validate

hirelog:
  worker:
    job-summary:
      enabled: true

# application-prod.yml
app:
  storage:
    base-dir: /data/hirelog/storage